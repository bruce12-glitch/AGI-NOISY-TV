# Notebooks

The tabular suite is intentionally script-first so a clean checkout can be
reproduced without notebook state. Exploratory notebooks can be added here;
use the CSV files in `results/raw/` and `results/summary/` as their inputs.
